# GitHub OAuth App 配置
# 注意：变量名必须带 PUBLIC_ 前缀（config.ts 用 import.meta.env.PUBLIC_* 读取）
# clientSecret 建议留空（Gitalk implicit OAuth 模式，避免 secret 泄露到前端 bundle）
PUBLIC_GITHUB_CLIENT_ID=your-github-client-id
PUBLIC_GITHUB_CLIENT_SECRET=
